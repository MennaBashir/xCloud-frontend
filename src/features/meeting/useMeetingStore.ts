/**
 * Compatibility shim — preserves the original default export path used by
 * AppLayout (`@/features/meeting/useMeetingStore`).
 */
export { useMeetingStore as default } from "./store/meetingStore";
